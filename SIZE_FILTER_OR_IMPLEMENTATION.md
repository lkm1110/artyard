# ✅ 크기 필터 OR 조건 구현 완료

## 📊 **구현 방식**

### **OR 조건** ⭐
가로 OR 세로 중 **하나라도** 범위에 포함되면 표시

```typescript
// src/services/artworkService.ts
if (filter?.sizeRange) {
  const { min, max } = filter.sizeRange;
  filteredData = filteredData.filter(artwork => {
    if (!artwork.size) return false;
    
    // size 형식: "50×70cm" 또는 "50 x 70 cm" 등에서 숫자 추출
    const numbers = artwork.size.match(/\d+/g);
    if (!numbers || numbers.length === 0) return false;
    
    // 추출된 숫자 중 하나라도 범위 내면 포함 (OR 조건)
    return numbers.some(num => {
      const size = parseInt(num);
      return size >= min && size <= max;
    });
  });
}
```

---

## 🧪 **동작 예시**

### **필터: 50-100cm**

| 작품 크기 | 결과 | 이유 |
|---------|------|------|
| 70×80cm | ✅ 표시 | 70과 80 모두 범위 내 |
| 70×120cm | ✅ 표시 | 70은 범위 내 (120은 밖) |
| 150×60cm | ✅ 표시 | 60은 범위 내 (150은 밖) |
| 150×120cm | ❌ 숨김 | 150과 120 모두 범위 밖 |
| 40×200cm | ❌ 숨김 | 40과 200 모두 범위 밖 |

---

## 🎯 **장점**

✅ **사용자 친화적**: 더 많은 검색 결과
✅ **유연한 검색**: 가로나 세로 중 하나만 맞아도 포함
✅ **다양한 형식 지원**: "50×70", "50 x 70", "50x70" 모두 처리
✅ **클라이언트 필터링**: DB 스키마 변경 불필요

---

## 📝 **지원 형식**

```
✅ "50×70cm"       → [50, 70]
✅ "50 x 70 cm"    → [50, 70]
✅ "50x70"         → [50, 70]
✅ "100×150×200cm" → [100, 150, 200] (3개 숫자도 OK)
❌ "No size info"  → 제외
❌ ""             → 제외
```

---

## 🔄 **필터 흐름**

```
1. DB에서 데이터 가져오기 (페이지네이션 적용)
   ↓
2. 크기 필터 적용 (클라이언트 사이드)
   - size 문자열에서 숫자 추출
   - OR 조건으로 필터링
   ↓
3. 좋아요/북마크 상태 조회
   ↓
4. 최종 데이터 반환
```

---

## 📊 **수정 파일**

```
✅ src/services/artworkService.ts
   - 크기 필터 OR 조건 구현
   - 클라이언트 사이드 필터링
   - actualCount 계산 수정
```

---

## 🧪 **테스트 방법**

### **1단계: 필터 열기**
```
Home 화면 → Filters 버튼 클릭
```

### **2단계: 크기 설정**
```
Size Range 슬라이더:
- Min: 50cm
- Max: 100cm
```

### **3단계: 적용**
```
Apply Filters 버튼 클릭
```

### **4단계: 결과 확인**
```
피드에 표시되는 작품들:
- 70×80cm ✅
- 70×120cm ✅ (70이 범위 내)
- 150×60cm ✅ (60이 범위 내)
- 150×120cm ❌ (둘 다 범위 밖)
```

---

## 💡 **추가 개선 가능 사항**

### **1. AND 조건 옵션 추가** (선택 사항)
```typescript
// 사용자가 선택할 수 있도록
interface SizeFilterOptions {
  range: { min: number; max: number };
  mode: 'OR' | 'AND'; // 사용자 선택
}

// AND 조건 예시
return numbers.every(num => {
  const size = parseInt(num);
  return size >= min && size <= max;
});
```

### **2. 가로/세로 별도 필터** (고급 기능)
```typescript
interface AdvancedSizeRange {
  width?: { min: number; max: number };
  height?: { min: number; max: number };
}
```

### **3. DB 인덱싱 최적화** (성능 개선)
```sql
-- 추후 성능이 중요해지면
ALTER TABLE artworks 
ADD COLUMN width_cm INTEGER,
ADD COLUMN height_cm INTEGER;

CREATE INDEX idx_artworks_width ON artworks(width_cm);
CREATE INDEX idx_artworks_height ON artworks(height_cm);
```

---

## ✅ **현재 상태**

```
✅ OR 조건 필터링 구현 완료
✅ 다양한 size 형식 지원
✅ 클라이언트 사이드 처리
✅ actualCount 정확히 계산
✅ 즉시 테스트 가능
```

---

**크기 필터 OR 조건 구현 완료!** 🎉

**테스트**: Filters → Size 50-100cm → Apply → 결과 확인!

